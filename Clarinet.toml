[project]
name = "federated-parse-interface"
authors = ["ParseTeam"]
description = "Decentralized parsing and data interaction interface"
telemetry = false
requirements = []
[contracts.federated-parser]
path = "contracts/federated-parser.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
